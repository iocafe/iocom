/* This file is generated by parameters_to_c.py script, do not modify. */
void ioc_initialize_candy_parameters(const struct candy_t *sigs, os_int block_nr, void *reserved)
{
  os_memclear(&ioc_prm_storage, sizeof(ioc_prm_storage));
  ioc_prm_storage.block_nr = block_nr;
  ioc_set(&sigs->exp.cam_nr, 1);
  ioc_set(&sigs->exp.img_width, 640);
  ioc_set(&sigs->exp.img_height, 480);
  ioc_set(&sigs->exp.framerate, 30);
  ioc_set(&sigs->exp.brightness, 50);
  ioc_set(&sigs->exp.saturation, 50);
  ioc_set(&sigs->exp.hlight_blink, 40);
  ioc_set(&sigs->exp.hlight_lvl, 0);
  ioc_set(&sigs->exp.on, 1);
}

osalStatus ioc_load_candy_parameters(const struct candy_t *sigs)
{
#if OSAL_PERSISTENT_SUPPORT
  os_char buf[21];
  osalStatus s;

  ioc_prm_storage.changed = OS_FALSE;
  s = os_load_persistent(ioc_prm_storage.block_nr, buf, sizeof(buf));
  if (!OSAL_IS_ERROR(s)) {
    ioc_write(sigs->exp.cam_nr.handle, sigs->exp.cam_nr.addr, buf + 0, 2, 0);
    ioc_write(sigs->exp.img_width.handle, sigs->exp.img_width.addr, buf + 2, 3, 0);
    ioc_write(sigs->exp.img_height.handle, sigs->exp.img_height.addr, buf + 5, 3, 0);
    ioc_write(sigs->exp.framerate.handle, sigs->exp.framerate.addr, buf + 8, 3, 0);
    ioc_write(sigs->exp.brightness.handle, sigs->exp.brightness.addr, buf + 11, 2, 0);
    ioc_write(sigs->exp.saturation.handle, sigs->exp.saturation.addr, buf + 13, 2, 0);
    ioc_write(sigs->exp.hlight_blink.handle, sigs->exp.hlight_blink.addr, buf + 15, 3, 0);
    ioc_write(sigs->exp.hlight_lvl.handle, sigs->exp.hlight_lvl.addr, buf + 18, 3, 0);
  }
  return s;
#else
  return OSAL_STATUS_NOT_SUPPORTED;
#endif
}

osalStatus ioc_save_candy_parameters(const struct candy_t *sigs)
{
#if OSAL_PERSISTENT_SUPPORT
  os_char buf[21];
  osalStatus s;

  ioc_read(sigs->exp.cam_nr.handle, sigs->exp.cam_nr.addr, buf + 0, 2, 0);
  ioc_read(sigs->exp.img_width.handle, sigs->exp.img_width.addr, buf + 2, 3, 0);
  ioc_read(sigs->exp.img_height.handle, sigs->exp.img_height.addr, buf + 5, 3, 0);
  ioc_read(sigs->exp.framerate.handle, sigs->exp.framerate.addr, buf + 8, 3, 0);
  ioc_read(sigs->exp.brightness.handle, sigs->exp.brightness.addr, buf + 11, 2, 0);
  ioc_read(sigs->exp.saturation.handle, sigs->exp.saturation.addr, buf + 13, 2, 0);
  ioc_read(sigs->exp.hlight_blink.handle, sigs->exp.hlight_blink.addr, buf + 15, 3, 0);
  ioc_read(sigs->exp.hlight_lvl.handle, sigs->exp.hlight_lvl.addr, buf + 18, 3, 0);
  s = os_save_persistent(ioc_prm_storage.block_nr, buf, sizeof(buf), OS_FALSE);
  ioc_prm_storage.changed = OS_FALSE;
  return s;
#else
  return OSAL_STATUS_NOT_SUPPORTED;
#endif
}

osalStatus ioc_autosave_candy_parameters(const struct candy_t *sigs)
{
#if OSAL_PERSISTENT_SUPPORT
  if (ioc_prm_storage.changed) {
    if (os_has_elapsed(&ioc_prm_storage.ti, 3000)) {
       ioc_save_candy_parameters(sigs);
    }
  }
  return OSAL_SUCCESS;
#else
  return OSAL_STATUS_NOT_SUPPORTED;
#endif
}

