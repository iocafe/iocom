14.3.2026 / pekka

The dynamicio directory contains dynamic data extensions for base iocom. 
These extensions allow controller to configure itself by signal/pin 
configuration from the IO device.

The dynamicio is compiled as part of the iocom library, nor a separate
extension library like most of iocom extensions.
